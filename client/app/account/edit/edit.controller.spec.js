'use strict';

describe('Controller: AccountEditCtrl', function() {
  // load the controller's module
  beforeEach(module('asrApp'));

  var AccountEditCtrl;

  // Initialize the controller and a mock scope
  beforeEach(inject(function($controller) {
    AccountEditCtrl = $controller('AccountEditCtrl', {});
  }));

  it('should ...', function() {
  });
});
